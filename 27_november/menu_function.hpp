#pragma once

namespace LAlex {
    struct MenuItem;
    
    const MenuItem* show_menu(const MenuItem* current);
    const MenuItem* exit(const MenuItem* current);
    const MenuItem* study_matanaliz_func(const MenuItem* current);
    const MenuItem* study_algebra_func(const MenuItem* current);
    const MenuItem* study_go_back(const MenuItem* current);
    const MenuItem* matanaliz_first_func(const MenuItem* current);
    const MenuItem* matanaliz_second_func(const MenuItem* current);
    const MenuItem* algebra_first_func(const MenuItem* current);
    const MenuItem* algebra_second_func(const MenuItem* current);



}
